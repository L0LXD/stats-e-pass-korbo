import numpy as np
from misc import *
from mean import *
low,high,X = grouping()
freq = np.array([])
#print(std_dev(X,freq))
#print(var(X,freq))
#print(freq_mean(X,freq))
#print(range_mean(X,freq))
#print(step_deviation(X,freq))
#print(median(low,high,X,freq,))
